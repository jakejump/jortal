services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jortal
    volumes:
      - jortal_pg_data:/var/lib/postgresql/data

volumes:
  jortal_pg_data:
